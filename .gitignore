node_modules
dist
.DS_Store
*.local
tsconfig.tsbuildinfo
exemplo-para-importar.json

# Credenciais do Firebase. As chaves web não são segredo (vão no bundle de
# qualquer jeito — quem protege os dados são as Security Rules), mas ficam
# fora do git para cada ambiente ter as suas sem conflito.
.env
.env.*
!.env.example

# Cache local do CLI do Firebase (registro do que já foi enviado no deploy).
.firebase/
